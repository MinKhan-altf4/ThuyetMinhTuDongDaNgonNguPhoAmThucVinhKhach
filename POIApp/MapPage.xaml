<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="POIApp.MapPage"
             Title="Map">

    <Grid RowSpacing="0" ColumnSpacing="0">
        <Grid.RowDefinitions>
            <!-- Row 0: Search bar -->
            <RowDefinition Height="Auto" />
            <!-- Row 1: Map area (contains map + overlay panels) -->
            <RowDefinition Height="*" />
            <!-- Row 2: Bottom POI chips bar -->
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- ── SEARCH BAR (Row 0) ── -->
        <Frame Grid.Row="0"
               Margin="10,8,10,4"
               Padding="10,0"
               HeightRequest="44"
               BackgroundColor="White"
               CornerRadius="22"
               HasShadow="True"
               ZIndex="10">
            <Grid ColumnDefinitions="Auto,*,Auto">
                <Label Grid.Column="0"
                       Text="🔍"
                       FontSize="16"
                       VerticalOptions="Center"
                       Margin="2,0,8,0" />
                <Entry x:Name="SearchEntry"
                       Grid.Column="1"
                       FontSize="14"
                       VerticalOptions="Center"
                       Completed="OnSearchCompleted"
                       TextChanged="OnSearchTextChanged"
                       BackgroundColor="Transparent" />
                <Button x:Name="BtnClearSearch"
                        Grid.Column="2"
                        Text="✕"
                        FontSize="13"
                        WidthRequest="32"
                        HeightRequest="32"
                        CornerRadius="16"
                        Padding="0"
                        BackgroundColor="Transparent"
                        TextColor="#9E9E9E"
                        Clicked="OnClearSearchClicked"
                        IsVisible="False" />
            </Grid>
        </Frame>

        <!-- ── MAP AREA (Row 1) ── -->
        <Grid Grid.Row="1" ZIndex="1">

            <!-- Map fills entire area -->
            <WebView x:Name="MapWebView"
                     ZIndex="1" />

            <!-- Status bar (floats on map) -->
            <Frame VerticalOptions="Start"
                   HorizontalOptions="Fill"
                   Margin="12,12,12,0"
                   Padding="10,8"
                   BackgroundColor="#E3F2FD"
                   CornerRadius="12"
                   HasShadow="False"
                   ZIndex="5">
                <VerticalStackLayout Spacing="2">
                    <Label x:Name="LblUserStatus"
                           FontSize="12"
                           TextColor="#0D47A1" />
                    <Label x:Name="LblNearestStatus"
                           FontSize="11"
                           TextColor="#455A64" />
                </VerticalStackLayout>
            </Frame>

            <!-- Search suggestions popup (floats on map) -->
            <Frame x:Name="SearchSuggestionsPanel"
                   VerticalOptions="Start"
                   HorizontalOptions="Fill"
                   Margin="12,66,12,0"
                   Padding="0"
                   BackgroundColor="White"
                   CornerRadius="12"
                   HasShadow="True"
                   IsVisible="False"
                   ZIndex="10">
                <ScrollView HeightRequest="260">
                    <VerticalStackLayout x:Name="SearchSuggestionsContainer" Spacing="0" />
                </ScrollView>
            </Frame>

            <!-- Floating controls (bottom-left, vertical stack: ZoomIn → ZoomOut → Refresh) -->
            <VerticalStackLayout VerticalOptions="End"
                                 HorizontalOptions="Start"
                                 Margin="12,0,0,12"
                                 Spacing="6"
                                 ZIndex="10">
                <Button x:Name="BtnZoomIn"
                        Text="+"
                        FontSize="20"
                        FontAttributes="Bold"
                        WidthRequest="48"
                        HeightRequest="48"
                        CornerRadius="24"
                        Padding="0"
                        BackgroundColor="White"
                        TextColor="#1565C0"
                        Clicked="OnZoomInClicked" />
                <Button x:Name="BtnZoomOut"
                        Text="−"
                        FontSize="20"
                        FontAttributes="Bold"
                        WidthRequest="48"
                        HeightRequest="48"
                        CornerRadius="24"
                        Padding="0"
                        BackgroundColor="White"
                        TextColor="#1565C0"
                        Clicked="OnZoomOutClicked" />
                <Button x:Name="BtnReloadPOI"
                        Text="🔄"
                        FontSize="18"
                        WidthRequest="48"
                        HeightRequest="48"
                        CornerRadius="24"
                        Padding="0"
                        BackgroundColor="White"
                        TextColor="#1565C0"
                        Clicked="OnReloadPOIClicked" />
            </VerticalStackLayout>

            <!-- POI Detail Panel (floats on map, highest z-index) -->
            <Frame x:Name="POIDetailPanel"
                   VerticalOptions="End"
                   Margin="12,0,12,12"
                   Padding="14"
                   BackgroundColor="White"
                   CornerRadius="18"
                   HasShadow="True"
                   IsVisible="False"
                   ZIndex="15">
                <VerticalStackLayout Spacing="10">
                    <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                        <Label x:Name="LblPOIName"
                               FontSize="18"
                               FontAttributes="Bold"
                               TextColor="#0D47A1"
                               VerticalOptions="Center" />
                        <Button Grid.Column="1"
                                Text="✕"
                                FontSize="13"
                                WidthRequest="30"
                                HeightRequest="30"
                                CornerRadius="15"
                                Padding="0"
                                BackgroundColor="#FFEBEE"
                                TextColor="#C62828"
                                Clicked="OnCloseDetailClicked" />
                    </Grid>

                    <Label x:Name="LblPOIDescription"
                           FontSize="13"
                           TextColor="#37474F"
                           MaxLines="4"
                           LineBreakMode="TailTruncation" />

                    <HorizontalStackLayout Spacing="10">
                        <Label x:Name="LblPOIDistance"
                               FontSize="12"
                               TextColor="#546E7A"
                               FontAttributes="Bold" />
                        <Label x:Name="LblPOICoords"
                               FontSize="11"
                               TextColor="#78909C" />
                    </HorizontalStackLayout>

                    <VerticalStackLayout Spacing="4">
                        <Label x:Name="LblAudioLangLabel"
                               FontSize="12"
                               TextColor="#607D8B" />
                        <Picker x:Name="LanguagePicker"
                                FontSize="13"
                                SelectedIndexChanged="OnAudioLanguageChanged" />
                    </VerticalStackLayout>

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="8">
                        <Button x:Name="BtnPlayAudio"
                                FontSize="13"
                                HeightRequest="42"
                                CornerRadius="21"
                                BackgroundColor="#FF9800"
                                TextColor="White"
                                Clicked="OnPlayAudioClicked" />
                        <Button Grid.Column="1"
                                x:Name="BtnPauseAudio"
                                FontSize="13"
                                HeightRequest="42"
                                CornerRadius="21"
                                BackgroundColor="#455A64"
                                TextColor="White"
                                Clicked="OnPauseAudioClicked" />
                    </Grid>

                    <Label x:Name="LblAudioStatus"
                           FontSize="11"
                           TextColor="#00897B" />

                    <ActivityIndicator x:Name="AudioLoading"
                                       IsRunning="False"
                                       IsVisible="False"
                                       Color="#FF9800"
                                       HorizontalOptions="Center" />

                    <Button x:Name="BtnNavigate"
                            FontSize="13"
                            HeightRequest="42"
                            CornerRadius="21"
                            BackgroundColor="#43A047"
                            TextColor="White"
                            Clicked="OnNavigateClicked" />

                    <!-- ═════════════════════════════════════════════════════════════ -->
                    <!-- THÊM MỚI: MENU MÓN ĂN SECTION (TTS MENU)                     -->
                    <!-- ═════════════════════════════════════════════════════════════ -->
                    <BoxView HeightRequest="1" Color="#E0E0E0" Margin="0,6,0,6" />

                    <Label FontSize="13"
                           FontAttributes="Bold"
                           TextColor="#0D47A1"
                           Text="📋 MENU MÓN ĂN" />

                    <!-- Danh sách món ăn -->
                    <ScrollView x:Name="MenuScrollView"
                                HeightRequest="150"
                                IsVisible="False">
                        <VerticalStackLayout x:Name="MenuItemsContainer"
                                             Spacing="6"
                                             Padding="0,4" />
                    </ScrollView>

                    <!-- Thông báo chưa có menu -->
                    <Label x:Name="LblMenuEmpty"
                           FontSize="12"
                           TextColor="#9E9E9E"
                           HorizontalTextAlignment="Center"
                           Margin="0,8"
                           Text="Chưa có menu" />

                    <!-- Loading indicator menu -->
                    <ActivityIndicator x:Name="MenuLoading"
                                       IsRunning="False"
                                       IsVisible="False"
                                       Color="#FF9800"
                                       HorizontalOptions="Center"
                                       Margin="0,8" />

                    <!-- TTS Menu buttons -->
                    <Grid x:Name="MenuButtonsGrid"
                          ColumnDefinitions="*,*"
                          ColumnSpacing="8"
                          IsVisible="False">
                        <Button x:Name="BtnPlayMenu"
                                FontSize="13"
                                HeightRequest="42"
                                CornerRadius="21"
                                BackgroundColor="#9C27B0"
                                TextColor="White"
                                Text="🔊 Phát menu"
                                Clicked="OnPlayMenuClicked" />
                        <Button Grid.Column="1"
                                x:Name="BtnStopMenu"
                                FontSize="13"
                                HeightRequest="42"
                                CornerRadius="21"
                                BackgroundColor="#455A64"
                                TextColor="White"
                                Text="⏹ Dừng menu"
                                Clicked="OnStopMenuClicked" />
                    </Grid>

                    <Label x:Name="LblMenuStatus"
                           FontSize="11"
                           TextColor="#9C27B0" />
                </VerticalStackLayout>
            </Frame>
        </Grid>

        <!-- ── BOTTOM: POI CHIPS (Row 2) ── -->
        <Grid Grid.Row="2"
              BackgroundColor="White"
              Padding="12,10"
              ZIndex="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Label x:Name="LblNearbyTitle"
                   Grid.Row="0"
                   FontSize="14"
                   FontAttributes="Bold"
                   TextColor="#263238"
                   Margin="0,0,0,6" />
            <ScrollView Grid.Row="1"
                        Orientation="Horizontal"
                        HorizontalScrollBarVisibility="Never">
                <HorizontalStackLayout x:Name="POIChipsContainer" Spacing="8" />
            </ScrollView>
        </Grid>
    </Grid>
</ContentPage>
